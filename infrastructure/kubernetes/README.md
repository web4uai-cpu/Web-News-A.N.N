# Kubernetes Manifests

Production deployment manifests for all A.N.N. microservices.

## Contents (planned)
- Namespace and RBAC
- Per-service Deployments + Services
- Ingress (NGINX / Traefik)
- ConfigMaps and Secrets
- HPA (Horizontal Pod Autoscaler) per service
- PersistentVolumeClaims for media storage

See `k8s/enterprise-stack.yaml` for the legacy monolith deployment.
